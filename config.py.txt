import os

# Telegram Bot Token
TELEGRAM_BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN", "YOUR_BOT_TOKEN_HERE")

# Bot Owner Wallet (for fee collection)
OWNER_ADDRESS = os.getenv("OWNER_ADDRESS", "0x...")
OWNER_PRIVATE_KEY = os.getenv("OWNER_PRIVATE_KEY", "0x...")

# Encryption Key for private keys
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY = os.getenv("ENCRYPTION_KEY", "GENERATE_NEW_KEY_AND_SET_THIS")

# HyperEVM RPC
HYPEREVM_RPC = "https://rpc.hyperliquid.xyz/evm"

# Swap Settings
DEFAULT_SLIPPAGE = 5  # 5%
MAX_SLIPPAGE = 20  # Max 20%
FEE_AMOUNT = 0.005  # HYPE